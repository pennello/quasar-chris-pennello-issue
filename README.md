Review constants at the top of `Main.java` and `applicationDefaultJvmArgs` at the end of `build.gradle` as well as the logging configuration oin `logback.xml`, then:

```sh
./gradlew > log.txt 2>&1
```
